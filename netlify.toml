[build]
  publish = "."
  command = "npm install && echo 'Dependencies installed'"

# Redirect root to the main HTML file
[[redirects]]
  from = "/"
  to = "/bukhosi_caption_generator.html"
  status = 200
